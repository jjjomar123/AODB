projection;
strict;
use draft;

define behavior for ZC_IataMaster_S alias IataMasterAll

{
  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
//  use action SelectCustomizingTransptReq;

  use association _IataMaster { create; with draft; }
}

define behavior for ZC_IataMaster alias IataMaster

{
  use update;
  use delete;

  use association _IataMasterAll { with draft; }
}