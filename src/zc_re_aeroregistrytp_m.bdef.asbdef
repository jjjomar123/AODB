projection;
strict ( 2 );
use draft;

define behavior for ZC_RE_AEROREGISTRYTP_M alias AeroRegistry
use etag
{
  use create;
  use update;
  use delete;

  use action fileUpload;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}