projection;
strict;
use draft;

define behavior for ZC_AerocuteMaster_S alias AerocuteMasterAll

{
  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
  use action SelectCustomizingTransptReq;

  use association _AerocuteMaster { create; with draft; }
}

define behavior for ZC_AerocuteMaster alias AerocuteMaster

{
  use update;
  use delete;

  use action CopyAerocuteMaster;

  use association _AerocuteMasterAll { with draft; }
}